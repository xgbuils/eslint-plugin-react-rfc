/** @type {import('eslint-doc-generator').GenerateOptions} */
const config = {
  ignoreConfig: ["all"],
  urlConfigs:
    "https://github.com/xgbuils/eslint-plugin-react-rfc/#shareable-configs",
};

module.exports = config;
